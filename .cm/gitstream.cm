# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  {% for item in filenames %}
  review_name_{{ item.name }}:
    if: 
       - true   
    run:
      - action: add-comment@v1
        args:
          comment: |            
             name: file name is {{ item.testfile }}
  {% endfor %}
    
filenames:
  - name: testfiles
    testfile: {{ source.diff.files | map(attr='new_file' , regex=/^\w+./) }}

