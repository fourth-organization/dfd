manifest:
  version: 1.0

automations:
  flag_duplicate_h1:
    if:
      - true
    run: 
      - action: request-changes@v1
        args:
          comment: | 
              The fileng {{ duplicate }} hu {{ duplicatetwo }}

duplicate: {{ source.diff.files | matchDiffLines(regex=r/<h1>/) | lenghth }}
duplicatetwo: {{ source.diff.files | filter( regex=r/<h1>/) | length }}
