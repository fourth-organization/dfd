manifest:
  version: 1.0

automations:
  review_slash_commands:
    if:
       - {{ is.requested }}
    run: 
      - action: request-changes@v1
        args:
          comment: | 
                 @bot-gitstream check all
is:
  requested: {{ (pr.comments | match(attr='content', term='') | some) }}
