manifest:
  version: 1.0

automations:
  enforce_html_title_length:
    if:
      - {{ files | extensions | match(term='html') | every }}
      - {{ source.diff.files | matchDiffLines(regex=r/(<title>([\w\W]{1,29})<\/title>)|(<title>([\w\W]{61,})<\/title>)/) | some }}
    run: 
      - action: request-changes@v1
        args:
          comment: | 
              Request change and aske the author to update the PR to follow recommended HTML title lengths.
              An optimum title contains characters between 30 and 60 characters.
